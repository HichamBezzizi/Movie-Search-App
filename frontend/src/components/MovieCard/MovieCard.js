import React from "react";
import "./MovieCard.css";

const MovieCard = movies => {

  console.log(movies)
  console.log(movies.movies)

  console.log(movies.movies.forEach(movie => console.log(movie)))

  return (
    <div>
    </div>
  );
};

export default MovieCard;
